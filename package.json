{
  "name": "tms",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/tms",
    "build:all": "rm -rf dist && bun build src/index.ts --compile --target=bun-darwin-arm64 --outfile=dist/tms-darwin-arm64 && bun build src/index.ts --compile --target=bun-darwin-x64 --outfile=dist/tms-darwin-x64 && bun build src/index.ts --compile --target=bun-linux-arm64 --outfile=dist/tms-linux-arm64 && bun build src/index.ts --compile --target=bun-linux-x64 --outfile=dist/tms-linux-x64",
    "test": "bun test",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0",
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
